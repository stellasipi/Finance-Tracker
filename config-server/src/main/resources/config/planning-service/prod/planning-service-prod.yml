spring:
  datasource:
    url: jdbc:postgresql://ft-planning-db:5432/ft_planning_db
    driver-class-name: org.postgresql.Driver
    username: ftdb_user
    password: ftdb_user
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      org:
        hibernate:
          dialect: PostgreSQLDialect

server:
  port: 8080

finance-tracker:
  security:
    client-secret: 1d8fc149-f130-4a4a-bfe9-3f36e11e87cc
    client-id: planning-service